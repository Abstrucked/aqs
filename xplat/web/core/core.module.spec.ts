describe('Core module generic test', () => {
  it('Should be true', () => {
    expect(true).toBeTruthy();
  });
});

/*
import { PklCoreModule } from './core.module';

describe('PklCoreModule', () => {
  it('should work', () => {
    expect(new PklCoreModule(null)).toBeDefined();
  });
});
*/
